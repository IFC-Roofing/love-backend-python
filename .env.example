# AWS
AWS_REGION=us-east-1
COGNITO_REGION=us-east-1

# All secrets loaded from AWS Secrets Manager:
# - love-backend/db          (host, port, database, username, password)
# - love-backend/cognito     (user_pool_id, client_id, client_secret)

# Redis (optional overrides)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0

# Optional (defaults shown)
# DEBUG=false
# PROJECT_NAME=Love Backend
# CORS_ORIGINS=["http://localhost:3000"]
# SESSION_TTL=86400  # 24 hours in seconds
